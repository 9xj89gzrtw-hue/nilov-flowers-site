# worklog.md — nilov-flowers-site

## Сессия W96 (24.09.2026): редизайн под 5cv.ru

**Задача:** сделать сайт в дизайне и логике блоков 5cv.ru (кроме отзывов — их не надо), всё управляемо из админки.

### Разведка (выполнено)
- Склонирован репо, изучена архитектура (PHP 8 + SQLite, settings-driven, admin allowlist+CSRF+history). Прод на SpaceWeb, деплой по push в main через GH Actions.
- Локальный стенд поднят: статический PHP 8.4.23 (`/home/z/.local/bin/php`) + pm2 (`ecosystem.config.js`) → `nilov-site` на **127.0.0.1:8123** (php -S + router.php). Сайт отвечает 200, БД сидирована.
- 5cv.ru исследован (page_reader + agent-browser + VLM-анализ скриншотов):
  - Токены: `--pink #ff4ea2`, `--pink-dark #e03a8a`, `--amber #f5b301`, `--amber-dark #d99400`, `--ink #1c1a1e`, `--ink-muted #6e6a72`, `--surface #fff`, `--surface-subtle #f6f6f8`, `--surface-warm #fbf5f8`, `--line #e7e5ea`; шрифт Montserrat; кнопки radius 16px (CTA — тёмный ink-фон, белый текст).
  - Блоки по порядку: (1) бар города «Санкт-Петербург? Да/Выбрать другой» (2) шапка: логотип-цветок, кнопка «Каталог» (тёмная пилюля), поиск («Розы, пионы, букет маме…»), город, телефон, иконки юзер/сердце/корзина (3) hero-бенто: большая фото-карточка с H1 белым на фото + белая CTA-пилюля «Выбрать букет»; справа жёлтая промо-карточка «Цветы по подписке» (4) чипы: Хиты / До 3500 / 3500–7000 / От 7000 / Премиум (5) сетки секций «Хиты продаж», «Сборные букеты», «Премиум — для особого случая», «В шляпной коробке», «До 3500 ₽», «Дополните букет 🎈» — заголовок + «Смотреть все» (жёлтый андерскор) + стрелки карусели (6) «Цветы по поводу» — плитки-чипы с фото/пастельными фонами (7) «Наши магазины в Петербурге» — 3 карточки адресов с рейтингом (8) SEO-текст (9) FAQ-аккордеон (10) «Журнал» — карточки статей (11) футер.
  - Карточка товара: портрет ~3:4, бейджи слева-сверху («Хит» amber-фон ink-текст; «Премиум» ink-фон белый текст), сердечко справа-сверху, белая круглая «+» кнопка снизу-центр, под фото: жирная цена, название, рейтинг звезда 4.8.
  - Отзывы — НЕ переносим (требование заказчика).
- Montserrat скачан как variable (wght 100–900, cyrillic+latin woff2) → `fonts/MontserratVariable-{cyrillic,latin}.woff2` (CSP: только self-hosted).

### План волн
- **T1 (оркестратор):** план, AGENTS.md, worklog, шрифты, pm2-стенд. ✅
- **T2-a:** `css/five.css` — дизайн-система 5cv (токены, шапка, hero-бенто, чипы, карточки, плитки поводов, магазины, журнал, FAQ, футер) + fonts.css Montserrat.
- **T2-b:** `index.php` + `partials/header.php`/`footer.php` — новая структура блоков 5cv (все тексты через setting() с дефолтами; hero-фото с фолбэком; промо-карточка).
- **T2-c:** `includes/db.php` (migrateSchema: новые settings-ключи) + `admin/settings.php` (новые поля: hero-промо, секции, магазины, журнал).
- **T2-d:** вторичные страницы (product.php, occasion.php, offer_new.php, help.php, policy_new.php, 404.php, track.php) в новом дизайне.
- **T3:** скриншот-ревью agent-browser + фиксы.
- **T4+:** волны независимых критиков (каждый «первый раз»), фиксы до одобрения.
- **T5:** финальный коммит+пуш (git diff ревью, без force).

### Заметки
- Экосистема JS (cart, order-form) сохраняется — меняется только HTML-каркас и CSS.
- Существующие фичи-тумблеры (feature_*) по умолчанию сохраняют значения; новые блоки получают свои тумблеры.
- Бейдж «Премиум»: добавить products.is_premium (миграция) + бейдж на карточке; бейдж «Хит» — по флагу или продажам (is_hit) — решено: products.is_hit.

## T2-c (24.09.2026, W96): миграции БД + админка для блоков 5cv — Task ID: T2-c, Agent: backend/админка

**Файлы:** `includes/db.php`, `admin/settings.php`, `admin/products.php` (только эти три).

### includes/db.php — migrateSchema()
- products: `is_hit INTEGER NOT NULL DEFAULT 0`, `is_premium INTEGER NOT NULL DEFAULT 0` — идемпотентно через PRAGMA table_info (паттерн show_in_upsell). Бейджи «Хит продаж»/«Премиум» на карточках + одноимённые секции витрины.
- `INSERT OR IGNORE INTO settings` (многострочный $pdo->exec, стиль файла) — 58 новых ключей с дефолтами:
  - Город/шапка: feature_citybar=1, citybar_text, city_label, search_placeholder, catalog_btn_text.
  - Hero-промо: hero_promo_enabled=1 + badge/title/text/btn_text/link; hero_delivery_card_enabled=1 + title/text.
  - Чипы/карусели: feature_chips=1, chips_price_low='3500', chips_price_high='7000' (строками — settings TEXT), feature_carousels=1.
  - Секции: feature_section_hits/premium/budget/addons=1 + заголовки/подписи (section_budget_title='Букеты до %s ₽' — %s подставит порог чипа); badge_hit_text='Хит', badge_premium_text='Премиум'.
  - Поводы/магазины/SEO: feature_occasions=1, occasions_title; feature_stores=0 + stores_title/sub + stores_1..3_title/text (пустые); feature_seotext=1 + seo_text_title + seo_text_body (4 честных абзаца про зоны/сроки/свежесть/оплату, разделители \n\n).
  - Журнал: feature_journal=0, journal_title + journal_1..3_title/text/link/image (пустые — блок не показывается, пока владелец не заполнит).

### admin/settings.php
- $keys (allowlist): +40 текстовых ключей + chips_price_low/high; после цикла — приведение к `(string)max(0,(int)…)` только если ключ пришёл; seo_text_body прогоняется через `sanitize_rich_text($v, 5000)` (whitelist `<a>`, как cookie-тексты).
- Чекбокс-список обработчика: +13 тумблеров (feature_citybar, hero_promo_enabled, hero_delivery_card_enabled, feature_chips, feature_carousels, feature_section_hits/premium/budget/addons, feature_occasions, feature_stores, feature_seotext, feature_journal) — cb_rendered-механика наследуется.
- Uploads: journal_1..3_image — saveUpload(IMG_UPLOADS_DIR) + deleteImage + чекбокс `journal_N_image_remove` (паттерн favicon), удаление попадает в $values.
- Рендер: новая секция-карточка `#s-5cv` «Витрина 5cv (новый дизайн)» (+ чип в оглавлении, scrollspy подхватывает автоматически): подразделы «Шапка и город», «Hero-бенто: промо-карточка и доставка», «Чипы цен и карусели», «Секции каталога: Хиты, Премиум, Бюджет, Дополнения», «Поводы, магазины, SEO-текст» (seo_text_body textarea rows=8), «Журнал» (3 статьи: title/text/link + обложка с миниатюрой и чекбоксом удаления). Чекбоксы «включено по умолчанию» рендерятся через `!== '0'`, выключенные (stores/journal) — `=== '1'`.

### admin/products.php
- Форма: чекбоксы «Хит продаж (жёлтый бейдж + секция «Хиты продаж»)» и «Премиум (тёмный бейдж + секция «Премиум»)» рядом с show_in_upsell; save-обработчик: `$isHit/$isPremium = isset($_POST[...]) ? 1 : 0`, колонки добавлены в UPDATE и INSERT (параметризовано :ih/:ip).
- Список товаров: бейджи «Хит» (amber #f5b301/ink) и «Премиум» (ink #1c1a1e/белый) рядом с названием — inline-стили по образцу бейджа «К корзине».

### Проверки
- `php -l` × 3 — без ошибок.
- curl `/` = 200 (миграция выполнилась), повторный запрос 200 — идемпотентно (settings 130 строк, дублей ключей 0).
- PRAGMA table_info(products): is_hit, is_premium присутствуют; товары с флагами 0 (дефолт).
- `SELECT key FROM settings WHERE key LIKE 'hero_promo%'` — 6 ключей; все 58 новых ключей на месте, spot-check значений (chips 3500/7000, feature_stores=0, feature_journal=0, seo_text_body 4 абзаца).
- GET /admin/settings.php без логина → 302 на login; /admin/products.php → 302. Логином не тестили (по ТЗ).
- /product/buket-iz-roz = 200.

### Отклонения/заметки
- Полный список новых ключей — 58 (в ТЗ перечислено 58, сверено; в одной промежуточной проверке ошибочно считал 59).
- css/fonts.css менялся параллельно агентом T2-a — не трогал.
- Сид в seedDatabase() не расширял: migrateSchema() идемпотентен и выполняется для новых и существующих БД (согласовано с ТЗ).

---
Task ID: T2-a
Agent: CSS-дизайнер
Task: Дизайн-система 5cv (css/five.css + Montserrat + head.php)

Work Log:
- Прочитаны AGENTS.md + worklog.md (W96), style.css/nilov.css/fonts.css, partials/head.php|header.php|footer.php, js/cookie-banner.js — инвентаризация классов и JS-контрактов (#cartToggle, .cart-toggle, .site-nav, .product-card__*, .faq-item и т.д.).
- css/fonts.css (append, существующее не тронуто): @font-face Montserrat variable 100–900 (cyrillic + latin, unicode-range как у Golos) + 'Montserrat Fallback' (local Arial, ascent-override:91%, descent-override:24%, size-adjust:101%) — метрический fallback против CLS.
- css/five.css (новый, ~40КБ, 365 правил-блоков): токены 5cv (pink/amber/ink/muted/surface/line, radius 16/24, wrap 1200) + ремап ЛЕГАСИ-токенов (--bg→#fff, --bg-alt→subtle, --ink-soft→muted, --rose-cta→#e03a8a, --mint/--blue/--cream и т.д.) — старые секции (форма, футер, drawer, cookie, hero-фолбэк, marquee) перекрашиваются без правок PHP.
- Шрифты: --font-ui/--font-display = Montserrat; продублированы на html и html:root (специфичность 0,1,1 > :root в fonts.css, который грузится ПОСЛЕ five.css — иначе fonts.css вернул бы Golos; вариант только html из ТЗ не побеждает :root).
- Компоненты fc-*: fc-citybar (тёмный + amber «Да, верно» + ghost), fc-header (+ рестайл .site-header, sticky/white .95/blur 8), fc-hero бенто (2fr 1fr, скрим, стеклянная eyebrow-пилюля, белая CTA-пилюля, amber-промо + surface-warm «Доставка», фолбэк-градиент с SVG-цветком), fc-chips (+ рестайл .catalog-tabs в скролл-ряд пилюль), fc-row/fc-carousel (snap, скрытый скроллбар, clamp(220px,23vw,280px) / 72vw mobile), fc-occasions (фото-скрим + пастели по nth-child), fc-stores, fc-seo (columns:2 ≥900px), fc-faq + рестайл .faq-item (плюс/минус через ::after, open → бордер pink), fc-journal, fc-section (--subtle чередование), fc-product/fc-related/page-hero/prose (контракт под T2-d).
- Рестайл существующего: .product-card (белая, тень 0 1px 2px, hover -2px; медиа 3/4 surface-subtle radius 16; бейджи --hit amber / --premium ink / --sale pink / --urgent white+ink-бордер / --deliv warm; сердечко 36px c расширенной тап-зоной ::after; «+» белая 44px снизу-центр, hover pink; цена 800 1.1rem, имя .92rem 2-line clamp, категория .7rem uppercase), .btn/.btn--accent (ink, radius 16, hover #33303a, active translateY(1px), sheen из nilov погашен), .order-form (карточка radius 24 + grid 2 колонки ≥720px, поля surface-subtle + focus pink), .site-footer (ink-фон, розовые hover, пилюли мессенджеров rgba(255,255,255,.1); инлайн-цвета footer.php перекрыты !important для читаемости на тёмном), .mnav (blur + safe-area), .cart-panel__drawer (radius 24 0 0 24, items surface-subtle), cookie-баннер (amber accept / ghost), тонкий скроллбар, prefers-reduced-motion для новых анимаций.
- partials/head.php: link five.css между nilov.css и fonts.css; preload MontserratVariable-cyrillic.woff2 (только cyrillic); theme-color #ffffff.
- Проверки: php -l OK; curl | grep -c five.css = 1; скобки 365/365, балансовые проверки синтаксиса чисто; agent-browser (компьют-стили): body Montserrat/white, шапка rgba(255,255,255,.95) sticky 72px, чипы/карусели/бенто соответствуют спеке; mobile 375: hero 1 колонка 480px, поиск второй строкой, телефон/город скрыты; скриншот-ревью VLM desktop+mobile+product — критических CSS-баг нет.
- FIX в процессе: мобильный MQ шапки стоял ВЫШЕ базовых fc-search/fc-header__phone правил — база перебивала его по порядку каскада; перенёс MQ после всех базовых правил fc-header (проверено вживую: flex-basis 100%, phone display none).

Stage Summary:
- Готово: css/five.css (полная дизайн-система 5cv ~40КБ: токены+ремап легаси, все fc-* компоненты, рестайл карточек/кнопок/формы/футера/drawer/cookie/вторичных страниц, a11y 44px-тап-зоны сохранены, reduced-motion), Montserrat в fonts.css с metric-fallback, head.php (подключение+preload+theme-color).
- Ключевые решения: html:root-дублирование шрифтовых токенов (fonts.css грузится последним и его :root победил бы html из ТЗ); ремап легаси-токенов вместо точечных правок; тёмный футер потребовал !important против инлайн-стилей footer.php (файл менять нельзя); MQ шапки перенесён после базы (каскадная ловушка равной специфичности).
- Совместимость с параллельными агентами подтверждена: T2-b уже использует классы-контракты (fc-header/fc-hero/fc-chips/fc-carousel/fc-occasions/fc-seo в живом DOM) — стили совпали, JS-контракты не тронуты.
- Для следующих волн: T2-b может обернуть текст «Каталог» в <span> (для сжатия в иконку <900px — сейчас текст сырой, правило .fc-catalog-btn span{display:none} не находит цель); активный чип — ставить aria-pressed="true" или .is-active; стрелки каруселей data-атрибутами не заданы — T3-скриншот-ревью покажет остаточные шероховатости (контент-пустоты в каруселях — вопрос сида T2-c).

---
Task ID: T2-b
Agent: PHP-шаблонщик витрины
Task: Редизайн витрины под блочную логику 5cv (index.php + partials/header.php + partials/footer.php + js/five.js)

Work Log:
- Прочитаны AGENTS.md + worklog.md (W96, план волн), вся затрагиваемая периферия: util.php (setting/e/sanitize_rich_text/productPrice), db.php (migrateSchema: show_in_upsell есть, is_hit/is_premium может не быть → все чтения через ?? 0), js/catalog-filter.js (контракты вкладок/цены/empty-reset), js/nilov.js (hero__deadline + NILOV_CONFIG + petals/nvBrand — проверил, что их удаление из разметки безопасно: оба хука под if), cart-cta.js/cart-ui.js (data-order-cta, cartToggle).
- partials/header.php — полная перезапись под fc-header: логотип (контракт logo_image+logo_enabled → img, иначе inline SVG 5-лепестковый цветок var(--pink) 28px + span shop_name), кнопка «Каталог» (.fc-catalog-btn, грид-иконка 18px + текст в span — T2-a просил span для мобильного сжатия), поиск .fc-search (role=search, #fcSearch, placeholder setting('search_placeholder'), inline-иконка лупы — five.css позиционирует .fc-search svg), город (.fc-header__city, пин-SVG + city_label), телефон (header_phone с фолбэком на shop_phone, tel:), иконки .fc-header__icons: сердечко-ссылка на #catalog + КНОПКА КОРЗИНЫ скопирована байт-в-байт (#cartToggle/#cartCount/data-cart-mode). Skip-link сохранён.
- index.php — перестроен полностью, порядок блоков: (0) <head> не тронут: meta/OG, JSON-LD Florist, LCP-preload hero.webp, partials/head.php → (1) город-бар .fc-citybar (feature_citybar, #fcCityYes/#fcCityClose/#fcCitybar, текст citybar_text) → (2) header → (3) hero-бенто .fc-hero (fc-hero__grid 2:1; fc-hero__main[ --fallback] с picture/webp/dims + .fc-hero__img и оверлеем .fc-hero__content: eyebrow-пилюля, H1 = setting('seo_h1', hero_title) — по-буквенный stagger и nvBrand УДАЛЕНЫ, sub, CTA-пилюля со стрелкой, таймер .hero__deadline ИЗ ПОЛНОСТЬЮ + NILOV_CONFIG; fc-hero__side: жёлтая промо-карточка .fc-hero__promo[ -eyebrow/-title/-text/-btn] (hero_promo_*) + .fc-hero__delivery[-icon/-body/-title/-text] (hero_delivery_*)) → (4) чипы .fc-chips (feature_chips; 5 кнопок data-chip=hit|low|mid|high|premium c data-min/data-max из chips_price_low/high=3500/7000, number_format, aria-pressed; счётчик .fc-chips__count aria-live) → (5) карусели (feature_carousels): render_fc_row() = .fc-section>.wrap>.fc-row(.fc-row__head: title/sub/link/arrows + .fc-carousel); «Хиты продаж» (is_hit=1, фолбэк первые 8; feature_section_hits), по каждой активной категории с ≥1 товаром (до 12, data-tab={id} у «Смотреть все»), «Премиум» (is_premium=1, фолбэк топ-3 по productPrice; feature_section_premium), «До N ₽» (productPrice ≤ chips_price_low, заголовок sprintf(setting('section_budget_title','До %s ₽')); feature_section_budget), «Дополните букет» (show_in_upsell=1 в try/catch → пропуск на старой БД; feature_section_addons) → (6) каталог #catalog (ВСЁ старое сохранено: catalog-tabs, #priceFilter+пороги, favToggle, zoneCheck*, #catalogGrid, #catalogEmpty+Reset; заголовок/подпись через catalog_title/subtitle) → (7) «Цветы по поводу» #occasions (feature_occasions; occasions → плитки fc-occasion--photo/--pastel, первое фото из product_ids, подпись без хвоста « в Санкт-Петербурге» (preg_replace), ссылка /occasion/{slug}) → (8) форма заказа #order БЕЗ изменений внутри, обёртка fc-section--subtle → (9) магазины .fc-stores (feature_stores + stores_1..3_title/text, пустые title пропускаются) → (10) SEO-текст .fc-seo (feature_seotext; sanitize_rich_text(5000) → абзацы \n\n → <p>, т.к. five.css стилизует .fc-seo p, а не br) → (11) FAQ (существующий блок + JSON-LD как был, обёртка fc-section max-width 760, список в .fc-faq) → (12) журнал .fc-journal (feature_journal='0' по умолчанию ВЫКЛ; journal_N_title/text/link/image, карточки с fc-journal__media).
- Карточка товара вынесена в render_product_card(array $p, array $ctx): void (echo) — единый вид в каруселях и каталоге; добавлены data-hit/data-premium атрибуты и бейджи --hit/--premium (badge_hit_text/badge_premium_text), чтение флагов через (int)($p['is_hit'] ?? 0) — безопасно до миграции БД.
- УДАЛЕНО (по ТЗ 5cv): marquee, trust-strip (был if false), how-it-works (шаги остаются в настройках, не выводятся), блок отзывов Яндекс Карт, $guarantees/$yandexReviews* переменные. Комментарии-памятки оставлены в коде.
- partials/footer.php: контент ВЕСЬ сохранён (реквизиты, мессенджеры, cart-panel drawer со всеми id, promo, скрипты); изменения: mnav «Как работаем» → «Поводы» → /#occasions (id=occasions на секции поводов есть), после catalog-filter.js подключён <script src="/js/five.js">.
- js/five.js (новый, vanilla, 'use strict', DOMContentLoaded-safe через ready()): (1) город-бар — клик #fcCityYes/#fcCityClose → localStorage 'fc-city-ok'=1 + data-citybar-hidden + display:none, при загрузке скрытие сразу; (2) карусели — стрелки .fc-row__arrow скроллят .fc-carousel на clientWidth*0.8 (smooth / auto при prefers-reduced-motion), disabled-состояния по scroll-событию (scrollLeft<=4 / >=scrollWidth-clientWidth-4) + resize + отложенные перепроверки; (3) чипы+поиск — ОБЩЕЕ состояние (AND), скрытие через data-fc-filtered="1" + инжект CSS-правила [data-fc-filtered]{display:none!important} из JS (css/* трогать нельзя) — не конфликтует с catalog-filter.js (его is-hidden/style.display живут отдельно, итоговая видимость = произведение); чип: hit → data-hit=1, premium → data-premium=1, low/mid/high → границы (min эксклюзивно, max включительно) из data-min/data-max самого чипа; поиск #fcSearch по submit (Enter) + сброс при очистке крестиком (input): фильтр по подстроке .product-card__name, скролл к #catalog; empty-state: свой пересчёт (getComputedStyle + is-hidden + data-fc-filtered) → #catalogEmpty.hidden, при пустом поиске временная подмена заголовка «По запросу «…» не нашлось» (оригинал в dataset.origTitle); счётчик .fc-chips__count = «N букетов» (рус. склонение) по активному чипу; reset #catalogEmptyReset дополнительно снимает чип+поиск (остальные фильтры сбрасывает catalog-filter.js); (4) .fc-row__link[data-tab] — клик активирует вкладку каталога (tab.click()) для соответствия якоря #catalog.
- Интеграция с five.css (T2-a приземлился параллельно): сверил все селекторы; ПОДПРАВИЛ разметку под фактическую дизайн-систему: hero-контент в .fc-hero__content-оверлей + img.fc-hero__img; промо-бейдж → .fc-hero__promo-eyebrow; доставка → .fc-hero__delivery-icon/-body обёртки; чипы — .fc-chips сам является скролл-рядом внутри .wrap; .fc-occasions/.fc-stores/.fc-seo — контейнеры-сетки, а не секции; FAQ-список в .fc-faq; журнал — .fc-journal__media; текст «Каталог» в span; лупа в .fc-search. Подписи секций premium/budget/addons читают section_*_sub с дефолтом '' (ключи уже в БД от T2-c).
- Проверки: php -l index.php/header.php/footer.php = OK; node --check five.js = OK; curl / = 200 (16 уникальных fc-* классов в выводе), /product /occasion /offer /policy /track /js/five.js = 200; pm2-err.log — единственный Warning (забытый $featDeliveryBadge) пойман и исправлен, новых нет. agent-browser (функциональный прогон): город-бар скрывается + персистит в localStorage через reload; чип low → «3 букета», скрытие карточек, empty-state; чип+поиск AND → 0 видимых → «По запросу «xyz» не нашлось»; reset снимает чип/поиск/вкладку/цену, возвращает 3 карточки и исходный заголовок empty; карусель: стрелка скролл 0→286, prev/next disabled по краям; data-tab-ссылка включает вкладку категории; вторичные страницы: #fcSearch без каталога → form.action='/#catalog', five.js загружен, mnav-ссылки верны; ошибок JS/консоли нет. VLM-скриншот-ревью: все блоки на месте, критики вёрстки нет (замечания VLM = сид-данные: 1 товар в категорийных каруселях; cookie-баннер — легаси-компонент).

Stage Summary:
- Готово: витрина перестроена под блочную логику 5cv (город-бар → header → hero-бенто → чипы → 5 видов каруселей → каталог → поводы → форма → магазины → SEO → FAQ → журнал), единая render_product_card для каруселей и каталога, js/five.js (город-бар/карусели/чипы/поиск поверх catalog-filter.js без конфликтов).
- Ключевые решения: фильтрация five.js через data-fc-filtered + инжект CSS-правила из JS (не воюем за style.display с catalog-filter.js — фильтры перемножаются); SEO-абзацы \n\n → <p> (five.css стилизует .fc-seo p, nl2br не попал бы под селектор); картинка hero внутри picture с классом fc-hero__img (абсолютное позиционирование работает сквозь статический picture); таймер/NILOV_CONFIG и вся форма заказа перенесены без единой правки содержимого.
- Гейты: новые блоки управляются feature_citybar/chips/carousels/section_hits/section_premium/section_budget/section_addons/occasions/stores/seotext/journal (ключи уже в БД от T2-c; defaults в коде — работает и на старой БД).
- Для следующих волн (T2-d/T3): product.php/occasion.php ещё держат старые hero-классы (nv-hero и пр.) — five.css уже содержит рестайл .site-header и interim-правила для старого hero; на карточках каталога reveal.js затемняет ниже фолда на full-page скриншотах (легаси, не баг); карусели с 1 товаром выглядят пусто — лечится сидом/наполнением (T2-c/admin).

---
Task ID: T2-d
Agent: PHP-шаблонщик вторичных страниц
Task: Вторичные страницы под дизайн 5cv (product.php, occasion.php, offer_new.php, policy_new.php, help.php, 404.php, track.php, order-thanks.php)

Work Log:
- Прочитаны AGENTS.md + worklog.md (W96, отчёты T2-a/T2-b/T2-c), css/five.css (§18 «Вторичные страницы»: page-hero/prose/breadcrumbs/fc-product/fc-related), index.php (render_product_card/render_fc_row как эталон), js/product-gallery.js, js/five.js (стрелки каруселей биндятся по .fc-row→.fc-carousel), js/lightbox.js, js/cart-cta.js, js/reveal.js, style.css (legacy-галерея, sticky-CTA, nth-of-type-каскад бейджей), partials/head|header|footer.php.
- product.php: `<main id="main" tabindex="-1">` → fc-section > wrap; breadcrumbs → паттерн «простые ссылки с текстовым „ / “» (aria-current на текущей); сетка .fc-product (2 колонки ≥960px, gap 40) = .fc-product__gallery.product-gallery (sticky сохранён) + .fc-product__info.product-page__info (flex-gap раскладка сохранена, five.css класса для info-колонки нет — оставлен легаси-контейнер). Галерея: контракт product-gallery.js не тронут БАЙТ-в-байт (#productGalleryTrack/.product-gallery__slide ×2/#productGalleryCounter/[data-gnav]/#productGalleryThumbs .product-gallery__thumb) + добавлен класс fc-product__thumb (64×64, розовая рамка активной); при feature_gallery=0 фолбэк .product-page__media → +fc-product__media (4/5, radius 24, surface-subtle) с сохранением data-lightbox-trigger/src/alt. СКРИПТ-ГАЛЕРЕЮ НЕ оборачивал fc-product__media: overflow:hidden из five.css перебил бы overflow-x:auto viewport (каскад равной специфичности) — свайп бы сломался; legacy-галерея уже перекрашена §18 five.css (radius 24, розовый aria-current, subtle-подписи).
- Инфо-колонка: category → .product-card__cat (eyebrow), бейджи 5cv в ряд (sale pink −N% / hit amber / premium ink / urgent white+ink) — классы .product-card__badge--* как на витрине + точечный style="position:static" (класс absolute — в потоке пилюлями; каскадный nth-of-type-стек на галерее не подходит для инфо-колонки); H1 → .fc-product__title; цена → .fc-product__price (800/1.6rem, --old зачёркнута, --discount pink); urgent-note .product-card__urgent-note; описание → .prose; CTA — кнопка БЕЗ изменений контракта (btn btn--accent + fc-product__cta + product-page__cta--sticky, все data-product-*, IntersectionObserver-скрипт и .product-page__cta-spacer сохранены); мета-блок ul.fc-product__meta с .fc-product__meta-icon (36px круг surface-warm, svg pink): грузовик→delivery_badge_text, пин→«Самовывоз: pickup_address||shop_address», карта оплаты→существующий ykOn-текст (бывший .cart-note), гарантии $trust (guarantee_1..6 + фолбэк guarantees) с иконками камера/цветок/щит по циклу — весь контент старой страницы сохранён.
- Related «С этим берут»: fc-section.fc-related > .fc-row (head: .fc-related__title + .fc-row__arrows) + .fc-carousel; карточки render_related_card() — витринный вид (media-link, webp-picture, бейджи sale/urgent/hit/premium, .product-card__cta с data-order-cta, name/price с old/discount); запрос БД не менялся (SELECT * … LIMIT 3). Стрелки реально работают — их биндит js/five.js по .fc-row.
- occasion.php: fc-section > wrap; breadcrumbs-«/»; .page-hero (page-hero__title + .section-sub=intro); товары .catalog__grid с render_occasion_card() (витринные карточки: бейджи + «+» data-order-cta — кнопка была и раньше); body → fc-section--subtle > .prose (nl2br как было); FAQ → .fc-faq + .faq-item (рестайл five.css), заголовок setting('faq_title'); финальные CTA (btn--accent «Заказать с доставкой сегодня» / btn--outline «Весь каталог») в конце секции FAQ. JSON-LD CollectionPage и og-мета не тронуты. 404-ветка — центрированная карточка.
- offer_new.php / policy_new.php: doc-page-каркас (классов в CSS не было вообще) → fc-section > wrap; breadcrumbs; .page-hero (H1); ВЕСЬ юридический текст 1:1 внутри .prose (h2/p/ul/a стилизованы five.css); «Вернуться в магазин» → .btn.btn--accent; дата редакции — прежний точечный инлайн-стиль. $pageTitle задаётся до require head.php (twitter:title).
- help.php: был standalone без каркаса → переведён на partials/head+header+footer (наследует шапку/подвал/five.css); fc-section > wrap(820) + breadcrumbs + .page-hero; 10 разделов → .fc-store-карточки (одна колонка через инлайн-override сетки fc-stores) с .fc-store__title + .fc-store__text; локальный <style> ТОЛЬКО для kbd/таблицы/тег-пилюль (аналогов в five.css нет) — покрашены токенами 5cv (--surface-warm/--line/--amber); тег-пилюли rose→amber; контент всех разделов 1:1; фикс одной строки таблицы под новый hero (фото теперь большое слева, не «справа»); финал — .btn--accent «Открыть админ-панель». Убран баг: литеральный текст комментария W90 печатался в <head> вне <?php.
- 404.php: standalone → каркас сайта (head/header/footer, noindex,follow, статус 404 сохранён); fc-section + центрированная карточка max-width 560: гигантский «404» (pink, 800, clamp(4.5rem,16vw,7.5rem), точечный инлайн), .page-hero__title «Страница не найдена», .section-sub, .btn--accent «На главную».
- track.php: PHP-логика 1:1 (rate-limit rl_check + 429/Retry-After, нормализация 7/8, маскировка, LIMIT 5); title/meta перенесены ДО require head.php (было после — работало по автокоррекции парсера); fc-section > wrap(560) + breadcrumbs + .page-hero; форма → КАРТОЧКА .order-form (белая, radius 24) с .order-form__field (label+input уже перекрашены five.css: subtle-фон, розовый фокус) + label for/id trackPhone + .order-form__hint; submit .btn--accent.order-form__submit; инлайн grid-template-columns:1fr (перебивает 2-колоночность ≥720px). Результаты: track-card/track-card__status/track-steps — локальный <style> сжат и перекрашен токенами 5cv (new=warm/pink-dark, canceled=subtle/muted, шаги --pink); track-empty — пунктирная карточка.
- order-thanks.php: fc-section + центрированная карточка 560: галочка-SVG в круге 72px bg --surface-warm, штрих --pink (замена nv-bloom); .page-hero__title «Спасибо! Заказ №N принят»; тексты/кнопки («Выбрать ещё букеты» btn--accent, «Позвонить нам» btn--outline) и guard-редиректы id/существование заказа — 1:1. Замечено: локальный роутер перехватывает /order-thanks своей инлайн-страницей (order-thanks.php достижим как /order-thanks.php и на проде через .htaccess QSA) — проверен прямым вызовом файла.
- Проверки: php -l × 8 — без ошибок. curl: / = 200; /product/buket-iz-roz|vesennij-etyud|polevye-tsvety = 200; /product/nonexistent-x = 404; /occasion/svadebnye-bukety|buket-na-den-rozhdeniya|traurnye-kompozicii = 200; /offer = 200; /policy = 200; /help.php = 200; /track и /track?phone=… = 200; /nonexistent-404 = 404; /order-thanks.php?id=1 (временный тест-заказ, создан и удалён) = 200, ?id=556 = 302 на /. pm2-err.log — 0 новых Warning (6 старых — от сессии T2-b до их фикса).
- agent-browser (функциональный прогон product/buket-iz-roz): computed — fc-product 560px×2/gap 40, цена 25.6px/800 ink, CTA ink radius 16, мета-icon surface-warm, viewport overflow-x:auto (скролл жив), thumbs 64×64, галерея sticky, related-карусель flex×2 карточки; клик миниатюры №2 → счётчик «2 / 2», aria-current, scrollLeft=560; клик «+» в related → cartCount=1 + тост «Весенний этюд — в корзине (1 товар)»; клик главного CTA → cartCount=2 (корзина очищена после теста); mobile 390px: fc-product 1 колонка, CTA fixed (sticky-мобайл), скролл галереи жив; /product/vesennij-etyud: бейдж «Акционная цена −16%» position:static pink, old-price line-through, discount pink. Фолбэк-ветка feature_gallery=0 (тумблер временно выключен и возвращён): data-lightbox-trigger=1, fc-product__media=1, трек отсутствует. VLM-скриншот-ревью (desktop full + mobile + related + sale + help + track + 404 + occasion): галерея 4:5/счётчик/миниатюры с розовой рамкой, мета-иконки, карусель со стрелками и бейджем −16%, страницы повода/404/track/help — без дефектов вёрстки; «пустые фото в related на full-page» — артефакт lazy-load при сшивке (в viewport фото есть, naturalWidth 582/600), как на витрине (T2-b отмечал).

Stage Summary:
- Готово: все 8 вторичных страниц переведены на дизайн-систему 5cv через существующие классы five.css (fc-section/wrap/page-hero/prose/breadcrumbs/fc-product/fc-related/fc-carousel/fc-faq/fc-store/order-form/product-card__badge--*); PHP-логика всех страниц сохранена 1:1 (запросы, settings, JSON-LD, rate-limit, guard-редиректы, sticky-CTA-скрипт).
- JS-контракты (свято): [data-order-cta] (главный CTA + related/occasion карточки — проверено кликами в живую), product-gallery.js (#productGalleryTrack/.product-gallery__slide/#productGalleryCounter/#productGalleryThumbs .product-gallery__thumb/[data-gnav] — байт-в-байт), lightbox.js (data-lightbox-trigger/src/alt в фолбэк-ветке), cart-ui (#cartToggle из header), five.js (стрелки related-карусели работают через .fc-row).
- Ключевые решения: 1) fc-product__media НЕ вешается на скролл-viewport галереи (overflow:hidden five.css vs overflow-x:auto style.css — равная специфичность, поздний файл побеждает, свайп бы умер) — legacy-галерея уже перекрашена §18 five.css; 2) бейджи в инфо-колонке как пилюли (style="position:static") вместо absolute-каскада — absolute в непозиционированной колонке улетел бы к body; 3) help.php переведён со standalone на общий каркас (иначе page-hero/prose/fc-store из five.css недоступны), локальный <style> оставлен только для kbd/таблицы/тегов; 4) track: форма в .order-form-карточке, инлайн grid-template-columns:1fr.
- Для следующих волн (T3/T4): cookie-баннер на full-page скриншотах «перекрывает» контент (fixed + сшивка — легаси-компонент, отмечали и раньше); related-фото в full-page выглядят пустыми из-за lazy-load (в viewport грузятся, data-проблема сида: у 1 из 3 товаров есть webp-пара); роутер отдаёт /order-thanks (без id) своей инлайн-страницей в старом дизайне — на проду .htaccess ведёт на order-thanks.php, при желании унифицировать в T3; сид-фото содержит ценник 1450 ≠ цена БД 2500 (данные, не код).

---

## Сессия W96-fix1 (24.09.2026): фиксы по отчёту бизнес-критика — волна 1

**Задача:** 15 находок враждебного бизнес-критика после редизайна 5cv (F1–F15): пустой FAQ, мёртвая кнопка «Каталог», поиск без морфологии, лукавые дефолты промо-карточки/SEO-оплаты, счётчик «51 букет», нечестные «Смотреть все», отсутствие траст-сигналов на входе, клубника на траурной странице, фото с ценником, карусель из 1 товара, опечатки/канцелярит, устаревший /help.

**Файлы:** `includes/db.php`, `js/five.js`, `js/catalog-filter.js`, `partials/header.php`, `index.php`, `css/five.css` (только append-секция W96-fix1), `product.php`, `offer_new.php`, `help.php`, локальная БД, `img/products/roz2.{jpg,webp}`. Не тронуто больше ничего; не коммитил (пуш — волна оркестратора).

### Что сделано (по фиксам)

- **F1 (CRITICAL, FAQ пуст):** W96-витрина читает faq_q*/faq_a* с дефолтом `''` — ни один ключ никогда не попадал в БД (faq-тексты жили только в дефолтах старого index.php). В W96-блок `INSERT OR IGNORE INTO settings` добавлены faq_title + faq_q1..4 + faq_a1..4. Вопросы — «заводские» из settings-history.php (settingsDefaults), ответы написаны честно: доставка/цена, сроки до 20:00 + срочные 1–2 ч, фото перед отправкой, оплата наличными/онлайн-если-доступен. Ключи в allowlist админки уже были (faq_q1..faq_a4 в $keys) — FAQ сразу редактируемый. Локальная БД — INSERT OR IGNORE напрямую.
- **F2 (MAJOR, мёртвая «Каталог»):** partials/header.php: `.fc-catalog-btn` href="#catalog" → "/#catalog"; иконка-сердечко «Избранное — в каталоге» → "/#catalog". На главной это same-document скролл, со вторичных — переход к каталогу.
- **F3 (CRITICAL, «Розы» не находятся):** js/five.js — стемминг запроса (regex окончаний из ТЗ, срез оставляем ≥3 симв. — примеры ТЗ «розы»→«роз», «маме»→«мам» дают 3-симв. хвост; порог «≥4» из текста ТЗ противоречил его же примерам — примеры признаны приёмкой). Индекс: article.product-card получает `data-search` = mb_strtolower(name + category_name + description) — добавлен в render_product_card() (index.php) и render_related_card() (product.php, категория из SELECT * → ?? ''). Матч: каждый стемм слова запроса — подстрока data-search (AND). LIVE-фильтрация: input + debounce 250 мс без Enter; Enter — применить + скролл; пусто — снять фильтр. Перенос со вторичных страниц: форма action="/" method="get" (name="q" уже был) + JS submit → location.href='/?q='+encodeURIComponent(q)+'#catalog' (без JS — нативный submit тоже уводит на главную). На главной при загрузке читается URLSearchParams q → заполняется input + применяется фильтр (скролл делает hash).
- **F4 (MAJOR, «подписка» которой нет):** db.php W96-миграция: hero_promo_badge «Выгодно»→«Всегда», hero_promo_title «Цветы по подписке»→«Открытка в подарок», hero_promo_text→«Напишем ваш текст от руки и вложим в букет — бесплатно, в каждом заказе», hero_promo_btn_text «Подробнее»→«Оформить заказ», hero_delivery_title «Доставка 1–2 часа»→«Доставка в день заказа», hero_delivery_text→«По Санкт-Петербургу — оформите до 20:00, привезём сегодня». Дефолты setting() в index.php приведены в соответствие. Локальная БД обновлена PDO UPDATE (значения уже были записаны миграцией W96).
- **F5 (MAJOR, «онлайн картой или СБП»):** db.php дефолт seo_text_body + локальная БД: «Оплатить можно онлайн картой или через СБП…» → «Способ оплаты выберете при оформлении: наличными курьеру при получении или онлайн — если доступен в заказе.» Аналогично выправлен код-фолбэк $seoTextDefault в index.php (там была своя вариация той же лжи).
- **F6 (MAJOR→ счётчик «51 букет»):** js/catalog-filter.js ценовой фильтр выбирал `.product-card[data-price]` по всей странице (карточки каруселей + каталог = 51) И прятал карточки в каруселях. Селектор → `#catalogGrid .product-card[data-price]`. Чип-счётчик five.js уже считал только grid — согласовано.
- **F7 (MINOR):** render_fc_row() получил параметр $chip: секции Хиты→data-chip="hit", Премиум→"premium", До N→"low" (категорийные data-tab остались). js/five.js: клик по .fc-row__link[data-chip] делает chip.click() (полный путь чипа: is-active + aria-pressed + фильтр + скролл), уже активный чип — только якорь #catalog.
- **F8 (MAJOR, траст-ряд):** index.php после hero-бенто перед чипами — ul.fc-trust (гейт $heroTextEnabled, array_slice($guarantees,0,3), галочка в круге inline-SVG); $guarantees читается из guarantee_1..3 (дефолты — заводские из settings-history). css/five.css — append-секция «W96-fix1» со стилями fc-trust (+мобайл 699px).
- **F9 (MINOR, клубника на трауре):** сид occasions traurnye ':p'=>'3,9'→'3,16' (3 «Полевые цветы» остаются, 9 «Мини-набор клубники в шоколаде» → 16 «Красный акцент»: красные розы — уместно; «Белая нежность» тоже подходила, но 2 пункта ближе к ТЗ). Локальная БД UPDATE.
- **F10 (MINOR, фото с ценником «1450»):** сгенерировано roz2.jpg (z-ai image, 864×1152, «no text, no price tags» — VLM-проверка чистоты пройдена) + roz2.webp (GD, q82). UPDATE products SET image='roz2.jpg' WHERE slug='buket-iz-roz'; сид W96-демо: 'roz.jpg'→'roz2.jpg'.
- **F11 (MINOR, карусель из 1 товара):** index.php: секция «Дополните букет» рендерится при count($addonProducts) >= 2. UPDATE products SET show_in_upsell=1 WHERE slug='vesennij-buket-tyulpany' (+ сид) → клубника + тюльпаны = 2 карточки.
- **F12 (MINOR, «свежных»):** сид traurne intro «свежных»→«свежих» + UPDATE локальной БД replace().
- **F13 (MINOR, канцелярит оферты):** offer_new.php «Для текущей редакции действует редакция Политики, указанная на странице „Политика конфиденциальности“» → «Сроки и условия — в актуальной редакции Политики конфиденциальности» (ссылка /policy сохранена).
- **F14 (MINOR, /help устарел):** строку таблицы «Шаги 1–3 | Блок „Как это работает“» заменена на живую «Заголовок и подпись каталога | Секция „Каталог“» (шаги не выводятся — упоминание удалённого блока убрано, маппинг стал честным); «Гарантии 1–3 | Строка преимуществ…» → «Траст-ряд под hero и страница товара» (F8 вернул ряд); оборванное «карты важнее поиска для цветочного» дописано: «…: по фразе „розы“ поиск покажет только часть — карточки товаров и поводы точнее».
- **F15 (NIT):** сид: «Букет живых цветов из 7 альстромерий микс» → «Букет из 7 альстромерий микс» (других «живых цветов» в сиде нет — grep). Локальная БД UPDATE replace().

### Миграция для уже-развернутых БД (доп. к ТЗ, по паттерну w96_redesign_applied)

W96 уже в origin/main → прод-БД могла получить старые лукавые значения (INSERT OR IGNORE уже отработал). В migrateSchema() добавлен one-time блок `w96fix1_applied`: guard-UPDATE «только при старом дефолтном значении» (не трогает тексты, изменённые владельцем) — F4/F5/F9/F10/F11/F12/F15 для существующих БД. Guard для F9 усилен: замена 3,9→3,16 только если id 9 действительно «клубник…» и id 16 существует/активен (каталог владельца на проде другой — его подборку не ломаем).

### Проверки

- `php -l` × 6 (db.php, index.php, product.php, header.php, offer_new.php, help.php) — чисто; `node --check` five.js/catalog-filter.js — чисто.
- curl 200: /, /product/buket-iz-roz, /occasion/svadebnye-bukety, /occasion/traurnye-kompozicii, /offer, /policy, /track, /help; повторные запросы / — 200 (идемпотентно; settings 142 строки, дублей ключей 0; w96fix1_applied=1).
- grep -c data-search на главной = 52 (каталог + карусели).
- agent-browser (функционально): FAQ 4 вопроса; траст-ряд 3 пункта (VLM по скриншоту hero: «ВСЕГДА / Открытка в подарок / … / Оформить заказ» + доставка без «1–2 часа»); поиск «розы» живьём (debounce) → 8 карточек («Букет из роз» находится), «пионы» → 2 (пиона/пионовидные), «букет маме» (AND) → 0 + empty-state, очистка → фильтр снят; Enter на главной → фильтр + скролл к каталогу; «Смотреть все» у Хитов → чип hit is-active, aria-pressed=true, счётчик «5 букетов», скролл; /?q=пион#catalog → input заполнен, 2 карточки; поиск со страницы товара → редирект /?q=пион#catalog; клик «Каталог» на /product/buket-iz-roz → /#catalog, scrollY == catalogTop (catalogTop=0); ценовой фильтр «до 2500» → «7 букетов» (было 51), «все» → 17; «Дополните букет» = 2 карточки (VLM); карточка «Букет из роз» без надписей на фото (VLM); мобайл 390px — траст-ряд виден (12.8px), горизонтального скролла нет.
- pm2 err-лог — PHP-ошибок нет.

### Отклонения/заметки

- **ПОСЛЕДУЮЩИМ ВОЛНАМ (deploy-blocker):** sw.js VERSION = 'w5a' НЕ поднимался (вне разрешённого списка файлов этой волны) — при деплое поднять (w5a→w5b), иначе Service Worker отдаст вернувшимся посетителям старые five.js/catalog-filter.js (JS-кэш у SW cache-first). Локально проверено с очищенным SW.
- Порог стемминга: в ТЗ текст «хвост ≥4 символов» противоречил его же примерам («розы»→«роз» = 3) — реализован ≥3 (примеры = приёмка).
- F14: строку «Шаги 1–3» заменили на «Заголовок и подпись каталога → Секция „Каталог“» (буквальная замена правой ячейки оставила бы ложь «шаги показываются в каталоге»).
- F9: вместо подсказанных ТЗ «2,8» (весенний этюд + розовые розы) взяты «3,16» (полевые + красные розы) — по критерию ТЗ «НЕсладкие/НЕпраздничные».
- faq_a1..4 НЕ добавлены в settings-history.php settingsDefaults() (файл вне разрешённого списка) — сброс «к заводским» вернёт вопросы, но не ответы (ответы остаются в БД; при желании добавить в следующей волне).
- Локальный стенд: скрипты/стили отдаются с диска (кэш только у SW — см. выше); БД обновлена и через прямой PDO, и (idемпотентно) миграцией w96fix1.

Task ID: W96-fix1
Agent: исправитель волны 1 (business-critic fixes)

---

## Сессия W96-fix2 (24.09.2026): фиксы по отчётам дизайн-критика и QA-критика — волна 2

**Задача:** 11 находок (F1–F11): плитки поводов 154px + дыра 660px, «волна» цен в каталоге, related без «Смотреть все»/галерея-обманка, узкий FAQ, иконки-привидения в траст-ряду, невнятный 429, рейт-лимит жгущий квоту невалидными попытками, поиск без счётчика, кривая панель фильтров, mnav safe-area, воздух под hero на мобиле.

**Файлы:** `css/five.css` (append-секция «W96-fix2»), `index.php`, `product.php`, `js/order-form.js`, `js/five.js`, `api/orders.php`. НЕ менялись: `js/product-gallery.js` (уже guard-safe), `includes/security.php` (обошлись существующим rl_check/rl_retry_after). Не коммитил (пуш — оркестратор).

### Что сделано (по фиксам)

- **F1 (major, поводы):** `.fc-occasions`: `repeat(auto-fill,minmax(150px,1fr))` → `repeat(auto-FIT,minmax(260px,1fr))` + gap 16px. ВАЖНО: auto-fit, а не auto-fill из ТЗ — auto-fill при 3 плитках оставляет пустой 4-й трек (дыра ~290px), auto-fit схлопывает пустые треки → 3 плитки делят контейнер поровну (замер: 3×376px при wrap 1160, заполнение до правого края — критерий ТЗ «~386px, ряд заполнен» выполнен). Плитка `aspect-ratio:4/3` (было 3/4) + `min-height:0` + `max-width:460px` (страховка от гиганта при 1-2 поводах); label 1rem. Mobile ≤699px: flex-ряд `overflow-x:auto` + `scroll-snap-type:x mandatory`, плитки `flex:0 0 44vw`, скрытый скроллбар, вписка в края (-20px margin / 20px padding). Замер 390: 3 плитки 172×129, scrollWidth 579 > 390.
- **F2 (major, волна цен):** замер вживую показал: `.product-card__name` УЖЕ выровнен (min-height:44px ≥ 2 строки 43.7px; line-clamp:2 работает; все имена = ровно 44.0px и в каталоге, и в каруселях). Предложенное в ТЗ `min-height:2.7em` (=39.7px) МЕНЬШЕ двух строк+padding — само создало бы волну 4px, поэтому НЕ добавлено (правило ТЗ «не дублируй конфликтно»). Реальный источник волны на мобиле — `.product-card__cat`, переносящийся на 2 строки («В шляпной коробке» 40.7px vs 20.3px у однокатоечных) → фикс: `white-space:nowrap;overflow:hidden;text-overflow:ellipsis`. После фикса замер 390: все cat=20.3px, priceTop попарно равны (7498/7498, 7849/7849…). Селекторы каталога и карусели идентичны (`.product-card__name`) — отдельного правила не нужно.
- **F3 (major, страница товара):**
  - a) Цены в related УЖЕ рендерятся и видимы (замер DOM: display:block, 3 цены 2 300/4 900/3 800 ₽, priceTop выровнен; VLM по скриншоту подтверждает) — находка QA-критика не воспроизводится на текущем коде (вероятно, старый SW-кэш CSS у критика). Ничего не ломал, зафиксировал в отчёте.
  - b) В шапку related-блока добавлена ссылка «Смотреть все» (`.fc-row__link`, href="/#catalog") — тот же класс/андерскор, что у каруселей витрины.
  - c) Галерея с ОДНОЙ картинкой: не рендерим счётчик «1 / 2» и блок миниатюр `#productGalleryThumbs` (две миниатюры одного файла = обманка «несколько фото»). Свайп/стрелки на честный слайд «Приближение этого же фото» остались. js/product-gallery.js НЕ трогал — он уже безопасен без thumbs/counter (`querySelectorAll`→пустой NodeList, `if (counter)`); проверено кликом: стрелка → scrollLeft 560, слайд 2 в кадре, 0 ошибок консоли. Появятся реальные вторые фото — блоки вернуть (комментарий в коде).
  - d) `.fc-product{margin:32px 0 0}` — был 0px воздух между крошками и сеткой (замер). Снизу страницу держит padding fc-section 56px desktop / 40px+58px body-под-mnav mobile.
- **F4 (minor, FAQ):** колонка 760→880px (в index.php, инлайн-стиль wrap), summary ярче: `.faq-item__q{font-weight:800;font-size:1.02rem}` + плюс темнее. Замер: faqW=880, weight=800.
- **F5 (minor, траст-ряд):** SVG в index.php заменён на заполненный круг `fill="currentColor"` + белая галочка (цвет круга наследует `color:var(--pink)` с существующего правила `.fc-trust__item svg`). Текст контрастнее: `.fc-trust__item{color:#4a4550}` (был muted #6e6a72). VLM: «заполненный розовый круг с белой галочкой».
- **F6 (high, невнятный 429):** js/order-form.js: в ветку не-ok добавлен кейс `res.status===429 || errors/error содержит rate_limited` → «Слишком много попыток оформить заказ. Подождите примерно N минут и попробуйте снова.» (N — из заголовка Retry-After, секунды → округление в минуты, min 1; русского склонения — хелпер pluralMinutes). Без Retry-After → «…обычно 5–10 минут…». Плюс `rate_limited` в карту ошибок (страховка от 429 без статуса через прокси). Протестировано на РЕАЛЬНОМ 429: «Подождите примерно 60 минут…», кнопка возвращается из disabled.
- **F7 (medium, рейт-лимит):** api/orders.php: было `rl_check('orders', 6, 3600)` на ЛЮБОЙ POST (QA словил 429 на 6-й невалидной попытке). Стало: (1) входной гейт `orders_attempts` 60/час — защита от флуда, не мешает живому покупателю; (2) квота созданных `orders_created` 12/час — проверка ПОСЛЕ полной валидации и ДО INSERT. Отклонение от буквы ТЗ («ПОСЛЕ успешного INSERT»): проверка после INSERT блокировать нечего (заказ уже сохранён), а rl_check при заполненном бакете ничего не пишет — квота никогда бы не сработала; позиция «после валидации, до INSERT» реализует интент «инкрементить только реальными заказами» (сюда не доходят невалидные 400/422/409). security.php не тронут. Прогон: 7 невалидных POST подряд → все 422 (не 429); 12 валидных → 201, 13-й → 429 + Retry-After:3600; невалидный после исчерпания created → 422 (не задет created-квотой).
- **F8 (minor, счётчик поиска):** js/five.js apply(): счётчик `.fc-chips__count` теперь общий для чипа и поиска — чип → «N букетов»; поиск (live и Enter — общий apply) → «по запросу «…» — N букетов»; чип+поиск → результат AND-фильтра (= total: всё реально видимое, включая вкладку/цену/избранное от catalog-filter.js); очистка/сброс → пусто. Убрана переменная chipVisible (считала только чип, игнорируя остальные фильтры). Прогон: «розы» → «по запросу «розы» — 8 букетов»; чип low + «роз» → «2 букета» (AND); очистка → «»; «xyzzy» → «0 букетов» + empty-state.
- **F9 (minor, панель фильтров):** index.php: два ряда (`.catalog-price-filter` отдельно + `.catalog-toolbar` ниже) слиты в ОДИН ряд `.catalog-toolbar`: `display:flex;justify-content:space-between;gap:10px 18px;flex-wrap:wrap`. Цена-группа слева, избранное в середине, zone-check справа (margin-left:auto сохранён). Заодно закрыт старый баг: закрывающий `</div>` toolbar-а рендерился безусловно (при выключенных favorites+zone открывался бы orphan-тег) — теперь открытие/закрытие в одном условии. ID/классы элементов не менялись — catalog-filter.js (цена/счётчик/empty/reset) и nilov.js (zone-check «Центральный» → «300 ₽») проверены живьём после реструктуризации. Замер 1440: один ряд y=6247 (price x191 → fav x429 → zone x903..1300); 390: аккуратный wrap в 3 ряда без наездов.
- **F10 (nit, mnav safe-area):** уже есть — `padding-bottom:env(safe-area-inset-bottom)` в five.css §16 (MQ ≤820px), computed проверен. Не менял.
- **F11 (nit, hero mobile):** замер 390: зазор side→trust = 2px + padding секции 8px; VLM по скриншоту: «большого пустого вертикального отступа нет». Пропущен по критерию ТЗ (только если >80px пустоты).

### Проверки

- `php -l` × 3 (index.php, product.php, api/orders.php) — чисто; `node --check` × 3 (order-form.js, five.js, product-gallery.js) — чисто.
- curl 200: `/`, `/product/buket-iz-roz`, `/product/buket-51-pion` (+ `/product/vesennij-etyud`, `/occasion/svadebnye-bukety`).
- agent-browser (СНАЧАЛА SW clear + reload): 1440 — поводы 3×376px до правого края (VLM: «плитки заполняют ряд до правого края»), траст-ряд розовые круги (DOM fill=currentColor + VLM), FAQ 880px/вес 800, фильтры в один ряд, цены каталога выровнены (VLM финального full-page: «цены визуально выровнены», «критических дефектов не выявлено»); товар — thumbs/counter отсутствуют, стрелки галереи живут, related «Смотреть все» + цены (VLM подтверждает); 390 — поводы горизонтальный скролл-ряд (scrollW 579>390, 3-я плитка обрезана краем = аффорданс свайпа), mnav 61px поверх контента, волна цен устранена (cat 20.3px везде, priceTop попарно равны), hero без пустоты.
- POST /api/orders: 7 невалидных `{}` → 422×7 (429 нет — attempts теперь 60/ч); квота created: 12×201 → 13-й 429 c Retry-After: 3600; тест-заказы удалены из БД (orders=0), rl-бакеты /tmp/rl_orders_* вычищены.
- Валидный заказ e2e через браузер: корзина (1× «Букет из роз») + форма («Тест Волна2», +7999…, зона, адрес, галка) → 201 → редирект `/order-thanks?id=17` (id 17 в БД: total 2800 = 2500 + зона 300, статус new) → заказ удалён из БД, `flowerCart` localStorage очищен.
- pm2 err-лог: PHP-ошибок/warning нет.

### Отклонения/заметки

- **ПОСЛЕДУЮЩИМ ВОЛНАМ (deploy-blocker):** `sw.js` VERSION = 'w5b' НЕ поднимался (файл вне разрешённого списка этой волны) — при деплое поднять (w5b→w5c): изменились five.js/order-form.js/index.php/product.php, а SW кэширует JS cache-first — вернувшимся посетителям достанется старый JS.
- F1: auto-FIT вместо auto-FILL из ТЗ — auto-fill при 3 плитках оставляет пустой трек (дыра ~290px справа), критерий приёмки ТЗ («3 плитки заполняют ряд, ~386px») выполняет только auto-fit.
- F2: `min-height:2.7em` из ТЗ НЕ добавлен — текущие 44px уже ≥ 2 строк (43.7px), а 2.7em (39.7px) меньше двух строк и создал бы волну 4px; реальная волна была от переноса строки категории — фикс эллипсисом.
- F3a: «related без цен» не воспроизводится (цены в DOM, display:block, VLM видит) — интерпретировано как устаревший SW-кэш у критика; SW-версия поднимется при деплое (см. blocker).
- F7: created-квота проверяется до INSERT, а не после (буква ТЗ) — после INSERT блокировать нечего, а rl_check не пишет при заполненном бакете (квота не сработала бы никогда); комментарий в коде поясняет.
- Все находки волны закрыты кодом; данные/БД не менялись (кроме создания/удаления тест-заказов).

Task ID: W96-fix2
Agent: исправитель волны 2 (design/QA-critic fixes)

---

## Сессия W96-fix3a (24.09.2026): технические фиксы по аудиту (perf/SEO)

**Задача:** T1–T6 — мёртвый Playfair-preload, srcset/sizes карточек (864px в слот 280px ≈ 3× перерасход), deflate+immutable-кэш, версионирование CSS, SEO-мелочи (sitemap/noindex/manifest/twitter), alt плиток поводов.

**Файлы:** `partials/head.php`, `sw.js` (VERSION не поднимал — оркестратор), `.htaccess`, `sitemap.php`, `manifest.webmanifest`, `index.php` (srcset-логика + alt + $pageDescription), `product.php` (только LCP-галерея), новые `img/products/thumbs/*-400.webp` (17 файлов, НЕ в .gitignore — коммитит оркестратор).

### Что сделано

- **T1 (Playfair):** удалены оба `<link rel="preload" … PlayfairDisplay-{cyrillic,latin}.woff2>` из partials/head.php (5cv использует только Montserrat; Golos-preload оставлен — fallback-стек fonts.css); sw.js STATIC_ASSETS: `/fonts/PlayfairDisplay-cyrillic.woff2` убран (файлы шрифтов в /fonts оставлены — @font-face в fonts.css и legacy-страницы админки; gzip-выигрыш: 2 preload'а по ~15-25КБ экономятся на каждой витринной странице).
- **T2a (srcset):** index.php — новые `product_img_thumb(array $p): string` (ленивая генерация `/img/products/thumbs/{имя без ext}-400.webp`: GD-ресайз до 400px, imagewebp q78, mkdir 0755, запись tmp+rename против гонок, PNG-альфа сохраняется, оригинал ≤400px / ошибка / не-писуемый каталог → '' с деградацией до прежнего вида; статик-кэш на запрос) и `product_img_width(): int` (честные {w}-дескрипторы: оригиналы 582–900px, «864w» для всех было бы ложью). render_product_card: `<source type="image/webp" srcset="thumb 400w, orig {w}w" sizes="(max-width:899px) 45vw, (min-width:900px) 300px, 280px">`; фолбэк-ветки сохранены (только webp-оригинал → прежний plain-srcset; ничего → source не печатается); img (jpg) без srcset — браузеры без webp получают оригинал как раньше. 800px-вариант не делал (по ТЗ достаточно 400+оригинал).
- **T2b (LCP товара):** product.php — главный снимок галереи (оба варианта: галерея и без feature_gallery) получил `loading="eager" fetchpriority="high"` (loading-атрибута НЕ было — eager по умолчанию, но без приоритета; width/height уже стояли). srcset галереи НЕ добавлял: слот ~580px, при DPR2 нужно ~1160px > 864 — превью 400w там не выбирается, оригинал уже оптимален (отметка в отчёте).
- **T3 (.htaccess):** mod_deflate (IfModule): DEFLATE для text/html text/plain text/css application/javascript application/json image/svg+xml text/xml application/xml application/xhtml+xml (картинки не жмём — форматы уже сжаты). CSS/JS кэш: ExpiresByType text/css|application/javascript 1h→1 year + FilesMatch `\.(css|js)$` → `Header set Cache-Control "public, max-age=31536000, immutable"` (правила картинок/woff2 не тронуты). Локальный php -S .htaccess не читает — gzip локально не проверяем (правило для прод-Apache); синтаксис сверен с каноничным сниппетом (h5bp), теги сбалансированы 10/10.
- **T4 (версионирование):** partials/head.php — все 4 CSS-ссылки получают `?v={md5_file 8 символов}` (паттерн favicon; @-guard, переменные считаются один раз в верхнем PHP-блоке). style.css/five.css/fonts.css — по ТЗ; nilov.css добавлен сверх списка ТЗ — иначе он один остался бы immutable-кэшируемым год БЕЗ версионирования (правки без инвалидации = стейл у не-SW посетителей). JS-ссылки не версионируются (живут в partials/footer.php — файл вне разрешённого списка; их свежесть держит поднятие sw VERSION при деплое).
- **T5 (SEO):** a) sitemap.php — /policy и /offer убраны (noindex-страницы в sitemap = mixed signals); b) order-thanks.php — `<meta name="robots" content="noindex">` УЖЕ стоял (строка перед require head.php, волна ранее) — не дублировал; c) manifest.webmanifest theme_color #F6F1E6→#ffffff (согласован с meta theme-color #ffffff в head.php); d) twitter:description — в head.php условный мета-тег из `$pageDescription` (печатается только если страница передала переменную до require); index.php передаёт `$pageDescription = setting('seo_description', …)` (админ-редактируемый, парно к og:description). product.php/occasion.php og:description формируют локально не через переменную — без передачи тег просто не печатается (TODO следующим волнам при желании).
- **T6 (alt поводов):** index.php — фото-плитки поводов `alt=""` → `alt="Букеты: {заголовок повода}"` («Букеты: Свадебные букеты» и т.д.).

### Проверки

- `php -l` × 5 (head.php, index.php, product.php, sitemap.php, order-thanks.php) — чисто; `node --check sw.js` — валиден.
- curl: `/` 200 (первый запрос 0.92с — генерация 17 thumbs, повторные 10-17мс); `/css/five.css` 200 и с `?v=` 200; `/sitemap.xml` — policy/offer отсутствуют (rg=0), 1 главная + 17 товаров + 3 повода; `/product/buket-iz-roz` 200 (fetchpriority="high" в HTML); `/manifest.webmanifest` 200 theme_color #ffffff; `/img/products/thumbs/roz2-400.webp` 200 image/webp 20766Б.
- agent-browser (SW unregister + caches clear → reload): карточки главной тянут `/img/products/thumbs/*-400.webp` (6 уникальных над folds, 200; полноразмерных оригиналов в network нет), errored-изображений 0, JS-ошибок 0, console чист; product/occasion страницы 200 без ошибок; VLM full-page скриншот: все карусели/каталог/поводы с фото, битых картинок нет.
- `ls img/products/thumbs | wc -l` = **17** (≥17 ✓; gen1-14 + roz2/p2/p3, по 20-38КБ против 70-195КБ webp/jpg-оригиналов ≈ −60-80% на карточку; overall видимый набор 2.2МБ→473КБ). Мусорных .tmp-файлов нет.
- PNG-альфа ветка GD проверена синтетическим тестом (600×800 RGBA → 400×533 webp, mime OK).

### Отклонения/заметки

- **ПОСЛЕДУЮЩИМ ВОЛНАМ (deploy-blocker):** `sw.js` менялся (Playfair убран из STATIC_ASSETS), но VERSION='w5c' НЕ поднимался (явно запрещено ТЗ — оркестратор) — при деплое поднять (w5c→w5d): изменились head.php (?v= меняет URL стилей — SW cache-first держит старые CSS) и index.php/product.php.
- **router.php (вне разрешённого списка):** локальный `/order-thanks` перехватывается router.php и рендерит свой standalone-HTML БЕЗ noindex (на проде Apache реврайт ведёт на order-thanks.php, где noindex есть) — при желании выровнять в следующей волне (require order-thanks.php вместо inline-HTML в роутере).
- product.php/occasion.php не передают $pageDescription (ограничение ТЗ «product.php — только галерея-LCP») — twitter:description сейчас только на главной; расширение — одной строкой на страницу при желании.
- journal-миниатюры alt="" оставлены (блок выключен по умолчанию, вне ТЗ).
- `product_img_webp()`: regex расширен `\.(jpe?g|png|webp)` — для webp-оригиналов (upload админки) возвращает тот же файл (поведение идентично прежнему, но явное).
- related-карточки product.php и карточки occasion.php srcset не получили (вне разрешённого списка файлов ТЗ) — тянут webp-оригиналы как раньше.
- Не коммитил (правило волны); thumbs не в .gitignore — оркестратору закоммитить `img/products/thumbs/` (17 файлов, ~473КБ).

Task ID: W96-fix3a
Agent: технический исправитель (perf/SEO audit fixes)

---

## Сессия W96-fix3b (25.09.2026): дизайн/UX-фиксы по волне критиков 3

**Задача:** D1–D11 (дизайн-критик 7/10 + покупатель 5.5/10): бейдж акции наезжает на сердечко, одноколоночный футер с 800px пустоты, контраст hero-заголовка 1.21:1, сердечко-иконка у «Каталога» в таббаре, перекрытие бейджей на товаре, нет «доставим сегодня» на товаре, нет телефона в мобильной шапке, бедная спасибо-страница, врущие счётчики, Montserrat в install-hint, «Смотреть все» ниже базовой линии.

**Файлы:** `css/five.css` (append-секция «W96-fix3b»), `partials/footer.php`, `partials/header.php`, `product.php`, `order-thanks.php`, `js/catalog-filter.js`, `js/five.js`. НЕ тронуты: db.php, index.php, sw.js, router.php, nilov.js, style.css/nilov.css. Не коммитил (пуш — оркестратор).

### Что сделано (по фиксам)

- **D1 (major, бейдж акции):** ≤699px `.product-card__badge{max-width:calc(100% - 92px);font-size:.68rem;padding:3px 8px}` — эллипсис сохранён, резерв справа 92px (отступ 10 + сердечко 40 на таче + тап-зона 10 + зазор). Замер 390px (карточка 168px): зазор badge.right→fav.left = **30px** у всех 3 скидочных карточек (было −2px). Текст «Акционная цена −16%» обрезается многоточием аккуратно (VLM подтвердил).
- **D2 (major, футер):** partials/footer.php переструктурирован: `.wrap.fc-footer__grid` = 4 колонки — brand (лого-цветок SVG как в шапке + `site-footer__name` + `setting('footer_about','Свежие букеты с доставкой по Санкт-Петербургу в день заказа')` + существующие пилюли wa/tg/vk/max/ig/email) / nav «Каталог» (категории `SELECT id,name FROM categories ORDER BY sort,id` → `href="/#catalog" data-tab={id}` + «Хиты продаж» `data-chip=hit`, заголовок из `section_hits_title`) / nav «Поводы» (occasions active ORDER BY sort LIMIT **6** — было 3, href=/occasion/{slug}, хвост « в Санкт-Петербурге» срезан как у плиток витрины) / «Контакты» (tel:, shop_hours, shop_address, mailto — только непустые). Нижняя строка: © год + реквизиты legal_* (перенесены из бренда, класс .fc-footer__legal) + track/policy/offer/cookie — как раньше. CSS: `.site-footer .fc-footer__grid{display:grid;grid-template-columns:1.4fr 1fr 1fr 1.2fr;gap:32px}` (селектор с `.site-footer` — перекрывает легаси `.site-footer .wrap{display:flex}` из style.css при равной специфичности, five.css позже); ≤899px — 2 колонки gap 24, ≤599px — 1. Заголовки колонок 700/.8rem/uppercase/.06em/rgba(255,255,255,.55); ссылки .85-white hover --pink; тексты ≥ .75-white на --ink (контраст ~10:1). footer_about НЕ добавлялся в БД (по ТЗ) — setting() с дефолтом в коде; в админку — следующей волне (allowlist).
- **D3 (major, hero-контраст):** append перекрывает скрим fix1: `.fc-hero__main::after{background:linear-gradient(90deg,rgba(28,26,30,.52),rgba(28,26,30,.12) 46%,rgba(0,0,0,0) 62%),linear-gradient(180deg,rgba(28,26,30,.04) 26%,rgba(28,26,30,.8) 100%)}` + `.fc-hero__title{text-shadow:0 2px 24px rgba(28,26,30,.45)}` + `.fc-hero__eyebrow{background:rgba(28,26,30,.42)}`. VLM 1440/390: заголовок, eyebrow и подзаголовок читаемы «даже на светлых участках», фото выглядит естественно (не пережжено).
- **D4 (minor, таббар):** у «Каталога» в .mnav сердце → грид-иконка (4 `<rect>` 3/3 7/7 rx1.5, stroke 1.6 — как у .fc-catalog-btn в шапке). Замер: 4 rect в DOM, VLM подтвердил «сетка из 4 квадратов, не сердечко».
- **D5 (minor, товар):** разбора grease: реальный источник — НЕ fixed-CTA, а **утёкший `top:88px`**: style.css даёт `.product-gallery{position:sticky;top:88px}` (десктоп), мобильный оверрайд меняет position:relative, но top остаётся → галерея сдвигалась вниз на 88px и позиционированная (relative) перекрывала верх инфо-колонки (бейджи/заголовок под фото + сверху fixed-CTA; замер: бейдж перекрыт на ~21px из 25px высоты). Фикс: `@media(max-width:820px){.product-gallery{top:auto}}`. После: галерея 297–749, инфо 773+ (24px gap), бейдж «Хит» чист (top 156, amber фон), H1/цена полностью видны; скроллом мета-список полностью раскрывается (lastLi.bottom 537 < ctaTop 722 при scrollY=900) — ничего не спрятано постоянно. Десктоп-стикли галереи (top:88) не тронут.
- **D6 (high, «доставим сегодня»):** product.php мета-блок — первая строка: иконка часов (circle r9 + стрелки) + `setting('product_today_text','Оформите до 20:00 — доставим сегодня')`; гейт `trim(...)!==''` (пустое ЗНАЧЕНИЕ в БД скрывает пункт — владелец выключает; отсутствующий ключ = дефолт). VLM desktop+390: строка первая в мета-списке с розовой иконкой-часами.
- **D7 (high, звонок в шапке):** partials/header.php — в .fc-header__icons первой идёт `<a class="fc-header__icon fc-header__call" href="tel:+{digits}" aria-label="Позвонить">` (Lucide phone path, stroke 1.8; digits = preg_replace \D от $headerPhone — как у .fc-header__phone). CSS: `.fc-header__call{display:none}` → `@media(max-width:899px){display:grid}` (круглая 40px как прочие). Замер 390: display:grid, 40×40, href=tel:+79000000000, aria «Позвонить», svg 20px; текстовый .fc-header__phone скрыт. Десктоп: иконка none, текст-телефон виден.
- **D8 (high, спасибо-страница):** order-thanks.php (guard-редиректы/noindex не тронуты): H1 с номером уже был; добавлены: строка-обещание `setting('thanks_call_text','Мы позвоним в течение 15 минут для подтверждения')` (гейт trim!==''), карточка «Что дальше» (.fc-thanks__steps, стилистика мета-блока товара: белая карточка/линия/круглые розовые иконки phone→camera→truck, 3 пункта: подтвердим по телефону / соберём и сфотографируем / привезём), кнопка «Отследить заказ» → /track (btn--outline, гейт feature_track_link). Существующие галочка и «Выбрать ещё букеты» сохранены. Проверено на /order-thanks.php?id=18 (локальный стенд: /order-thanks без .php перехватывает router.php — вне моего списка файлов, см. заметку fix3a).
- **D9 (high, счётчики):** реализовано через CustomEvent без coupling: js/five.js в apply() после каждой своей фильтрации (чип/поиск/сброс, в т.ч. стартовый ?q=) диспатчит `window.dispatchEvent(new CustomEvent('fc:filter',{detail:{visible:total}}))` (try/catch на старые браузеры); js/catalog-filter.js пересчитывает #priceFilterCount как «все видимые карточки #catalogGrid» (`!data-fc-filtered && !is-hidden && getComputedStyle(c).display!=='none'` — ловит вкладку+цену+чип+поиск+избранное) — и при своих изменениях (apply), и по событию. Прогон: чип low → чипы «11 букетов» И цена «11 букетов»; цена low → 8/пусто; чип hit + цена low (AND) → 1/1; поиск «роз» → 13/13; «xyzzy» → 0/0 + empty-state; reset → ''/23. Число «23» — все активные карточки сетки (оркестратор добавил товары), оба счётчика всегда совпадают.
- **D10 (nit, install-hint):** элемент рисуется js/nilov.js инлайн-стилями БЕЗ классов (файл вне списка) → CSS-перекрытие по атрибутам: `body > div[role="region"][aria-label="Установка приложения"] button{font-family:var(--font-ui)!important}` — инлайн `font:600 .8rem sans-serif` побеждён. Замер: fontFamily=Montserrat…, weight 600/12.8px сохранены. Body-паддинг под таббар: append `@media(max-width:820px){body{padding-bottom:61px}}` (замер mnav = 61px; было 58 — низ резался). cookie-visible-оверрайды не задеты (специфичность класса выше).
- **D11 (nit, «Смотреть все»):** `.fc-row__link{align-self:center}` — бокс ссылки центрирован в строке шапки секции, центр совпадает с центром стрелок (замер 695=695; было провисание на baseline заголовка 1.5rem).
- **five.js delegation (для D2-проверки):** rowLinks() переписан на делегирование кликов по `a[data-tab]`/`a[data-chip]` ЛЮБЫМ на странице (карусели витрины + футер; guard на модификаторы/средний клик). На главной клик применяет вкладку/чип (якорь #catalog — нативно); со вторичных страниц data-tab уводит на глубокую ссылку `/?category=N#catalog` (её подхватывает catalog-filter.js; замер: клик «Розы» из футера товара → /?category=1, вкладка активна, «5 букетов»), data-chip — просто к каталогу (нативный href).

### Проверки

- `php -l` × 4 (footer.php, header.php, product.php, order-thanks.php) — чисто; `node --check` × 2 (catalog-filter.js, five.js) — чисто.
- curl 200: `/`, `/product/buket-iz-roz`, `/product/vesennij-etyud`, `/order-thanks?id=1` (роутер-старница), `/order-thanks.php?id=18`, `/occasion/svadebnye-bukety`, `/track`.
- agent-browser (SW unregister + caches clear → reload, как всегда): 1440 — футер 4 колонки (323/231/231/277px, VLM: «все заполнены, пустоты нет, дефектов нет»), пилюли мессенджеров аккуратны (тест с временно включённым telegram/whatsapp, значения возвращены в БД в ''), клик «Хиты продаж» из футера → чип hit is-active + aria-pressed=true + ОБА счётчика «6 букетов» + скролл к каталогу; клик «Розы» из футера → вкладка активна, «2 букета» (вкладка+чип = AND); hero читаем (VLM); full-page 1440 VLM — «критических поломок нет» (серые плейсхолдеры в full-page скриншоте = незагруженные loading=lazy вне фолда, broken=0). 390 — шапка: иконка-трубка tel: (40×40, display:grid; текст-телефон display:none); бейджи акции: зазор 30px (≥8px) × 3 карточки, эллипсис аккуратен; hero читаем; mnav — грид-иконка (4 rect, VLM подтвердил); футер 1 колонка (350px), 760px — 2 колонки (348+348); body padding 61 = mnav 61; нижняя строка футера при макс. скролле выше таббара на 167px (DOM-замер, вопреки ошибке VLM). Товар 390 — галерея 297–749 / инфо 773+ (сдвига нет), бейдж «Хит» (top 156) и H1 полностью видны и не перекрыты (VLM ×2), мета раскрывается скроллом; «Оформите до 20:00 — доставим сегодня» — первая строка мета (DOM+VLM, desktop и 390); sticky-CTA поверх контента не конфликтует. /order-thanks.php?id=18 — №18, «позвоним в течение 15 минут», «Что дальше» ×3 (тексты в DOM), /track btn--outline, noindex на месте (VLM: карточка аккуратная, иконки не обрезаны). Счётчики — см. D9. JS-ошибок 0 на всех страницах (/, occasion, track, policy, offer, product ×2, order-thanks), console чист; pm2 err-лог — PHP-ошибок нет.
- Случайных правок чужих файлов нет: мои диффы только в 7 разрешённых файлах (прочие изменения в рабочем дереве — от fix3a/оркестратора, не трогал).

### Отклонения/заметки

- **ПОСЛЕДУЮЩИМ ВОЛНАМ (deploy-blocker):** изменились js/five.js + js/catalog-filter.js — SW cache-first отдаст вернувшимся посетителям старые JS; `sw.js` VERSION='w5c' НЕ поднимался (вне разрешённого списка) — при деплое поднять (w5c→w5d или актуальную следующую). CSS-five свежий через ?v= (head.php от fix3a), HTML — network-first: не страдают.
- **router.php (повтор заметки fix3a):** локальный `/order-thanks` (без .php) рендерит standalone-HTML роутера — мои изменения спасибо-страницы на стене видны только как `/order-thanks.php?id=N`; на проде Apache реврайт ведёт на order-thanks.php — там всё работает. Выровнять роутер — следующей волне (require вместо inline).
- Новые настройки НЕ в БД и НЕ в админ-allowlist (по ТЗ): `footer_about`, `product_today_text`, `thanks_call_text` — читаются setting() с дефолтами из кода; добавить в admin/settings.php $keys — следующей волне (1 строка на ключ).
- Каталожный скидочный бейдж рендерится БЕЗ модификатора `--sale` (index.php:212 — файл под запретом волны) → белая «стеклянная» пилюля вместо розовой 5cv, как на странице товара/в related. Исправить в index.php (добавить класс) — следующей волне; интервал/эллипсис от D1 корректны и для белого варианта.
- D11: align-self:center центрирует бокс ссылки (центр = центр стрелок); VLM придирается к визуальной массе андерскора (2px) — признано достаточным для NIT.
- Тестовые данные: временно включал shop_telegram/shop_whatsapp для скриншота пилюль — возвращены в '' (проверено). Заказы/rl-бакеты не трогал.

Task ID: W96-fix3b
Agent: W96-fix3b (дизайн-исправитель волны 3)

---
Task ID: W96-final
Agent: оркестратор (финал сессии)
Task: Финальные правки по вердиктам одобрения + закрытие сессии

Work Log:
- Финальная волна критиков (свежие, без контекста): дизайн-критик — ОДОБРЕНО, 9/10 (только minor/nit); критик-покупатель — ОДОБРЕНО, 8/10 (2 контент-majors с обходами).
- Реализованы рекомендации финальной волны (W96-fix4):
  - подборки поводов «Годовщина» (20,5,21 — юбилейный букет 4500₽ в подборке) и «Для любимой» (12,6,20) — сид + guard-миграция;
  - спасибо-страница: честная строка получения (LEFT JOIN зоны заказа: «Доставка: район «N»» вместо безусловного «Самовывоза») — e2e проверено;
  - delivery_slots: реальные интервалы «Утро/День/Вечер» (была пустая строка в БД → единственная опция «Любое время»);
  - min=today у поля даты;
  - шкала ценового селекта каталога синхронизирована с чипами (3500/7000, были 2500/4000);
  - «Открытка с вашим текстом — бесплатно» в мета-блоке товара.
- pm2-стенд nilov-site (127.0.0.1:8123) работал всю сессию без рестартов.

Stage Summary:
- Итог: дизайн 9/10 (критик-1), путь покупателя 8/10 (критик-2), техздоровье 7/10 (критик-3) — все финальные критики ОДОБРИЛИ.
- Деплой на прод идёт через GitHub Actions (4 успешных прогона за сессию), SW-версия w5d.
- Владельцу на проде: заполнить телефон/email/часы/реквизиты, магазины (feature_stores), журнал (feature_journal) — всё из админки «Витрина 5cv».

---

## Сессия W97 (25.09.2026): цикл враждебных критиков 1 + волна исправлений

**Цикл 1 (6 свежих критиков, без контекста прошлых волн):** дизайн 8.3, UX 7.8, покупатель 7.8, perf 7.5, SEO 7.8, a11y 8.2. Планка пользователя — каждый >9/10, циклы продолжаются.

**Исправлено (W97-fixA/B1/B2/B3a/B3b/C + оркестратор):**
- JS/UX: PWA-баннер (кнопка ×, 2-я сессия, выше CTA товара, не при cookie), единый фильтр избранного+цена (NfCatalogApply), пилюля поиска «Нашлось N — посмотреть» + автоскролл ?q=, pageshow-ресинк фильтров, клавиатурный лайтбокс, inert шапки/город-бара в drawer и cookie-настройках (фокус-ловушки), aria-live суммы, ?v= на ВСЕХ script (immutable-год), lazy-images.js отключён, условная загрузка скриптов по страницам.
- PHP-баги: /track поле телефона (коллизия $phone → $headerShopPhone), 429 честные минуты + лимит только на поиск, /help → admin/help.php (requireAdmin, кука path=/admin), 301 /index.php→/ и канонизация регистра/слэша витринных URL, прелоады Golos убраны (−62КБ), twitter:title/image синхронизированы, manifest в deflate.
- SEO: посадочные /category/{slug} (6 страниц, мета/JSON-LD/крошки/футер/«Смотреть все»/related), BreadcrumbList top-level у товара, FAQPage на occasion, title/description товара по шаблону, H1 «Доставка цветов по Санкт-Петербургу» (guard-миграция), WebSite+SearchAction, sitemap 35 URL (категории + lastmod из updated_at), og:image:width/height, описания 23 товаров 200-400 симв. (сид+guard).
- Perf: srcset thumbs у related/occasion/LCP товара (600w/900w), zoom-слайд ленивый (нет двойной загрузки .jpg), SW-прекэш очищен от мёртвых ключей (CSS убран — ключи ?v=, Golos убран), VERSION w5d→w5e.
- Дизайн/a11y: скрим hero (min контраст H1 2.26→4.64:1), чередование фонов секций + editorial-лента, розовая CTA (--pink-deep #cc2f7a, 4.94:1), имена карточек 15.5px/600 + min-height 46 (нет волны), бейдж скидки ink-on-pink (5.64:1), цена-дисконт --pink-deep, счётчик корзины --pink-deep, scroll-padding-top 96/170, фокус каруселей, моб-шапка 153→95px, чекбоксы 24px, ♥ у CTA товара (класс), карусели role=region aria-label tabindex=0, стрелки «{Секция}: вперёд», skip-link первый в DOM, цены зон в опциях («Центральный · 300 ₽»), дата max+60д, анонс нераспознанного района, allowlist: editorial_text/hero_image_alt.

**Стенд:** тумблеры feature_favorites/faq/price_filter/zone_check/track_link/gallery восстановлены в 1 (дефолты кода). Тест-заказы/rl-бакеты критиков вычищены.

**Хвосты (владельцу/следующим волнам):** телефон/email/часы/реквизиты/мессенджеры — заполнить из админки; yandex/google verification — владелец; AVIF-тир и дедупликация DOM каруселей (~100КБ HTML) — осознанно отложено (риск/польза).

---

## Сессия W98 (25.09.2026): цикл враждебных критиков 2 + волна исправлений

**Цикл 2 (6 НОВЫХ критиков):** редактор 8.2, CRO 7.4, безопасность 8.8, бизнес/админка 8.7, юрист 7.0, конкурент 6.3. Планка: каждый >9/10.

**Исправлено (W98-fixD/E/F + оркестратор):**
- Админка (D): планирование по дате доставки (колонка+фильтры+чипы «Сегодня/Завтра/7 дней/Без даты», export синхронизирован), чек-лист запуска на дашборде (8 динамических проверок + РКН-напоминания), «Инструкция» в меню, поле telegram_bot_token в профиле, роли: staff не видит Оплату/Уведомления/Push (серверный гард POST), таймзона MSK (SQLite localtime → PHP-даты), help.php честная инструкция, поиск по ~190 настройкам, «Вернуть в новые», confirm при удалении зоны, интервалы опроса честные.
- Тексты/право (E): интро 6 категорий ручными текстами (guard+allowlist, конец «Свежие В шляпной коробке»), SLA единый 15 минут, бейдж «Скидка N%», /track POST (телефон не в URL), оферта переработана (акцепт=заказ, отказ до передачи, без ограничения ответственности, фото до отправки, чек 54-ФЗ), политика (перечень данных/действий+IP, реестр РКН, вебвизор честно, статичная дата, «вы»), состав/масса/сроки сладких товаров, «каретта» и „лапки“ guard-правками, гарантии цветов скрыты у сладких, чекбокс согласия без bold с точным перечнем, зоны 3→8 (Петроградский/Василеостровский/Московский/Невский/Приморский), SEO-текст честный, hero-промо → #catalog, #orderEmptyState, offline-телефон формат, NBSP в PHP-ценах, title поводов с брендом.
- Security/CRO (F): .git/worklog/AGENTS/ecosystem — 410 на проде + исключены из rsync/tar деплоя, письмо-подтверждение КЛИЕНТУ (№, состав, сумма, обещание, /track), воронка Метрики (add_to_cart/cart_open/begin_checkout/purchase + window.__nfYmId, guarded), кнопка «В корзине ✓» + SR «Добавлено», NBSP в JS-ценах, пустая корзина на #order → заглушка с CTA.
- Оркестратор: passwordResetUrl через setting site_url (host-header injection), site_url в админке, SW VERSION w5e→w5f.
- Ложное срабатывание security-критика «branches: ain]» опровергнуто сырыми байтами (артефакт терминала, YAML валиден) — деплои идут (проверено GitHub API: 653c16d success).

**Хвосты (владельцу):** реальные контакты/реквизиты/часы/мессенджеры; уведомление РКН (+трансграничка Telegram); цели Метрики создать в интерфейсе; YooKassa включить при желании; тарифы 8 зон пересчитать под логистику; CSP-nonce осознанно отложен (~20 inline-скриптов, риск/польза).

---

## Сессия W99 (25.09.2026): регрессионный цикл критиков 3 (витрина) + фиксы

**Цикл 3 (6 свежих критиков):** дизайн 8.2, UX 8.8, покупатель 7.5, perf 8.4, SEO 7.5, a11y **9.3 (первый >9!)**. Ложное срабатывание дизайн-критика «скрима нет» опровергнуто (::after-градиент в five.css:989-990, критик смотрел ::before и сырые пиксели фото без композиции).

**Исправлено (W99-fixG1 PHP + W99-fixG2 JS/CSS):**
- PHP: meta_cut() по границе предложения (0/23 висячих предлогов), data-search и пр. убраны из карусельных копий (HTML −30КБ, −12%), hero srcset 480/768/1024w + preload imagesrcset, pwa-register.js ?v=, openingHoursSpecification из shop_hours (пусто — ключа нет), sitemap lastmod только реальные (0 фиктивных), title длинных ≤65, Florist sameAs из настроек, sr-only H2 «Доставка и оплата», ItemList на категориях, «Смотреть все» aria-label уникальны, img-ссылки карточек aria-hidden+tabindex=-1 (−60 Tab-стопов), track-steps «Шаг N из 3» + статусы без emoji, mnav aria-current, login «Вернуться в магазин», thanks: строка про письмо + сводка заказа, лейбл email честный, 3 editorial-врезки, «Дополните букет» без 🎈.
- JS/CSS: компактный моб-cookie-баннер 78px + body.nf-cookie-open поднимает sticky-CTA (elementFromPoint подтверждает клик в CTA), пилюля поиска скрывается по reset, сердце шапки включает фильтр избранного, стрелки галереи disabled по краям, «Нашёлся 1/21», chipsCount с контекстом запроса, пустой drawer без «Итого: 0 ₽», reduced-motion скроллы, чип↔селект цены синхронизированы, точный стемм-матч («роз»→10 роз, «розовый»→4, без подстрок), PWA не на order-thanks, сердечки/call 44×44, счётчик слайдов aria-live, related h2 24px, #order фон surface, safe-area, hero-CTA градиент #ff4ea2→#e03a8a (AA-large 3.06-4.08), sticky-CTA и «+» карточек → --pink-deep (единый primary), radius 16 унификация, editorial_text_2/3 в allowlist.
- SW VERSION w5f→w5g; тест-заказы 31-33 вычищены.

**Осталось (владельцу/осознанно):** демо-телефон/реквизиты/часы; минификация отложена (поддерживаемость > ~30КБ gzip); дедупликация DOM каруселей (следующий фикс: JS-рендеринг) отложена.

---

## Сессия W100 (25.09.2026): регрессионный цикл критиков 4 (бизнес-темы) + волна H

**Цикл 4 (6 свежих критиков):** редактор 8.7, CRO 8.3, security 8.4, бизнес/админка 9.0, юрист 8.0, конкурент 8.5. Противоречие критиков по „лапкам“ (W98 требовал, W100 отверг) — оставлены типографски правильные „лапки“ (внутренние кавычки).

**Исправлено (H1/H2 + оркестратор):**
- Тексты/право: финал оферты без бессмыслицы, составы сладких раздельно (без слэша/макарун-рассинхрона), «№ N» системно, «Цветы по поводам», figcaption «Общий вид/Крупный план», реквизиты-заглушки без бэк-лексики, футер — полное имя политики, title главной без тавтологии, FAQ «бесплатный», policy §2 действия ст.3 + §3 цель истории заказов + §5 честный Telegram-объём, оферта чек 54-ФЗ при любой оплате + возврат наличных, From noreply@прод, webvisor off по умолчанию (админ-чекбокс), cart_total_note пусто=авто-сноска от зон, пилюля «Отзывы на Яндекс Картах» при включении, zone-fallback текст.
- Админка/security: профиль — каналы только владельцу (+секреты не отдаются в DOM, пусто=не менять), история настроек с именами ключей + секреты маскируются «***» (undo не трогает), валидация scheme ссылок при сохранении (safe_url в util + применение на витрине: hero/promo/journal/VK/MAX/IG), чек-лист: Метрика обязательна + РКН ×2 + срочная-доставка-зона + демо-эвристика шире, staff: без чек-листа/меню «Сотрудники», thead в таблице заказов, @userinfobot подсказка.
- Оркестратор (хирургия): order-thanks BOLA закрыт (paymentToken = одноразовый ?t=, hash_equals, e2e: без токена 302), consent_log + версия политики, bootstrap-путь auth.php (concat-баг), push-subscribe rl 30/ч, .htaccess admin-bootstrap-*.txt deny, purchase-цель с промо-скидкой, aria-label CTA «Добавлено в корзину», апсейл add_to_cart, «товар/товара» терминология единая, компактный моб-город-бар (~52px), SW w5g→w5h.

**Хвосты (владельцу):** контакты/реквизиты/Мастер/ЮKassa/промокоды — чек-лист запуска ведёт; ночные слоты/подписка/бонусы — осознанно отложено (решение рынка).
